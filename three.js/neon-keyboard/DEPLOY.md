# 打包和部署指南

## 📦 打包步骤

### 1. 安装依赖（如果还没安装）
```bash
npm install
```

### 2. 构建生产版本
```bash
npm run build
```

构建完成后，会在项目根目录生成 `dist` 文件夹，包含所有优化后的静态文件。

### 3. 本地预览构建结果
```bash
npm run preview
```

在浏览器中打开显示的地址（通常是 `http://localhost:4173`）来预览打包后的效果。

## 🚀 部署方式

### 方式一：静态网站托管（推荐）

#### Vercel（最简单）
1. 安装 Vercel CLI：
   ```bash
   npm i -g vercel
   ```
2. 在项目目录运行：
   ```bash
   vercel
   ```
3. 按照提示完成部署

或者直接在 [Vercel](https://vercel.com) 网站：
- 连接你的 GitHub 仓库
- 选择项目根目录
- 构建命令：`npm run build`
- 输出目录：`dist`
- 自动部署完成

#### Netlify
1. 安装 Netlify CLI：
   ```bash
   npm i -g netlify-cli
   ```
2. 在项目目录运行：
   ```bash
   netlify deploy --prod --dir=dist
   ```

或者直接在 [Netlify](https://netlify.com) 网站：
- 拖拽 `dist` 文件夹到部署区域
- 或连接 GitHub 仓库自动部署

#### GitHub Pages
1. 安装 `gh-pages`：
   ```bash
   npm install --save-dev gh-pages
   ```
2. 在 `package.json` 添加脚本：
   ```json
   {
     "scripts": {
       "deploy": "npm run build && gh-pages -d dist"
     }
   }
   ```
3. 运行部署：
   ```bash
   npm run deploy
   ```

#### 其他静态托管服务
- **Cloudflare Pages**：连接 GitHub 仓库，构建命令 `npm run build`，输出目录 `dist`
- **Firebase Hosting**：使用 Firebase CLI 部署 `dist` 目录
- **AWS S3 + CloudFront**：上传 `dist` 目录到 S3，配置 CloudFront 分发

### 方式二：传统 Web 服务器

将 `dist` 文件夹的内容上传到你的 Web 服务器（Apache、Nginx 等）的网站根目录。

#### Nginx 配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## 📋 构建优化说明

项目已配置以下优化：

1. **代码分割**：React 和 Three.js 相关库分别打包，提高加载速度
2. **资源优化**：字体和 HDR 文件会自动复制到 `dist` 目录
3. **体积优化**：生产环境关闭 sourcemap，减小文件大小

## ⚠️ 注意事项

1. **字体文件**：确保 `public/fonts/OPPOSans-Regular.ttf` 存在，否则文字可能无法正确显示
2. **路径问题**：如果部署到子目录（如 `https://example.com/my-app/`），需要在 `vite.config.js` 中设置 `base: '/my-app/'`
3. **浏览器兼容性**：Three.js 需要现代浏览器支持 WebGL

## 🔍 检查清单

打包前请确认：
- [ ] 所有依赖已安装（`npm install`）
- [ ] 开发环境运行正常（`npm run dev`）
- [ ] 字体文件存在于 `public/fonts/` 目录
- [ ] 没有控制台错误

打包后请检查：
- [ ] `dist` 目录已生成
- [ ] `dist/assets` 包含 JS 和 CSS 文件
- [ ] `dist/fonts` 包含字体文件（如果使用了）
- [ ] 本地预览正常（`npm run preview`）

## 📊 性能建议

1. **启用 Gzip 压缩**：在服务器配置中启用 Gzip，可减少 60-70% 的文件大小
2. **CDN 加速**：使用 CDN 分发静态资源，提高全球访问速度
3. **懒加载**：如果场景很大，考虑实现模型和纹理的懒加载

## 🆘 常见问题

**Q: 打包后字体不显示？**
A: 检查字体路径是否正确，确保 `public/fonts/` 目录下的文件被正确复制到 `dist/fonts/`

**Q: 打包后页面空白？**
A: 检查浏览器控制台错误，可能是路径问题，尝试设置 `base` 配置

**Q: 打包文件太大？**
A: Three.js 项目通常较大，这是正常的。可以启用 Gzip 压缩，或考虑使用 CDN 加载 Three.js 库

