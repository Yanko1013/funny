# 🚀 快速打包指南

## 一键打包

```bash
npm run build
```

打包完成后，`dist` 文件夹就是可以部署的静态网站！

## 📁 打包结果

打包后的 `dist` 目录结构：
```
dist/
├── index.html          # 入口HTML文件
├── assets/             # JS和CSS文件
│   ├── index-xxx.js   # 主应用代码
│   ├── index-xxx.css  # 样式文件
│   ├── react-vendor-xxx.js    # React库
│   └── three-vendor-xxx.js    # Three.js库
├── fonts/              # 字体文件
│   └── OPPOSans-Regular.ttf
└── hdr/                # HDR环境贴图
    └── potsdamer_platz_1k.hdr
```

## 🌐 部署方式

### 最简单：Vercel（推荐）

1. 访问 [vercel.com](https://vercel.com)
2. 注册/登录账号
3. 点击 "New Project"
4. 导入你的 GitHub 仓库（或直接上传 `dist` 文件夹）
5. 构建设置：
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. 点击 Deploy，完成！

### 其他方式

- **Netlify**: 直接拖拽 `dist` 文件夹到 [netlify.com/drop](https://app.netlify.com/drop)
- **GitHub Pages**: 使用 `gh-pages` 包（详见 `DEPLOY.md`）
- **传统服务器**: 将 `dist` 文件夹内容上传到服务器

## ✅ 本地测试

打包后，先本地测试一下：

```bash
npm run preview
```

然后在浏览器打开显示的地址（通常是 `http://localhost:4173`）

## 📝 注意事项

- 确保 `public/fonts/` 目录下的字体文件存在
- 如果部署到子路径，需要修改 `vite.config.js` 中的 `base` 配置
- Three.js 项目文件较大是正常的，建议启用服务器 Gzip 压缩

## 📖 详细文档

更多部署选项和配置，请查看 `DEPLOY.md`

